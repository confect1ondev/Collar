# Collar Web - Environment Variables
# Copy to .env and configure for your deployment

# API URL (only needed if API is on different domain than frontend)
# If serving frontend and API from same domain, leave this commented out
# VITE_API_URL=https://leash.example.com/api
